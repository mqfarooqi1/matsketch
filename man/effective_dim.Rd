% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/nystrom.R
\name{effective_dim}
\alias{effective_dim}
\title{Effective dimension and recommended preconditioner rank}
\usage{
effective_dim(approx, mu)
}
\arguments{
\item{approx}{An object from \code{\link[=nystrom]{nystrom()}} or \code{\link[=rpchol]{rpchol()}}.}

\item{mu}{Positive regularization parameter.}
}
\value{
A list with \code{d_eff}, \code{recommended_rank}, and \code{sufficient}, which
is \code{TRUE} when \code{approx} already has at least the recommended rank.
}
\description{
Estimates \eqn{d_{\mathrm{eff}}(\mu) = \sum_j \lambda_j / (\lambda_j +
\mu)} from the eigenvalues of a low-rank approximation, and the rank
\eqn{2 \lceil 1.5\, d_{\mathrm{eff}} \rceil + 1} that Frangella, Tropp and
Udell show is enough for a well-conditioned preconditioner.
}
\details{
The estimate uses only the retained eigenvalues, so it can only
understate the true effective dimension. If the recommended rank exceeds
the rank of \code{approx}, build a larger approximation and ask again.
}
\examples{
set.seed(1)
X <- matrix(rnorm(1000), ncol = 2)
effective_dim(nystrom(kernel_matrix(X), l = 60), mu = 1e-2)
}
\seealso{
\code{\link[=nystrom_precond]{nystrom_precond()}}
}
