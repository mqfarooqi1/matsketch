% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/nystrom.R
\name{pcg}
\alias{pcg}
\title{Preconditioned conjugate gradients}
\usage{
pcg(A, b, mu = 0, precond = NULL, tol = 1e-08, maxit = 1000L, x0 = NULL)
}
\arguments{
\item{A}{A positive-semidefinite matrix, a function computing \eqn{A X},
or a lazy matrix from \code{\link[=kernel_matrix]{kernel_matrix()}} or \code{\link[=grm_matrix]{grm_matrix()}}. With
\code{mu = 0}, \eqn{A} must be positive definite.}

\item{b}{Right-hand side: a vector, or a matrix with one system per
column.}

\item{mu}{Non-negative regularization parameter.}

\item{precond}{\code{NULL} for no preconditioning, an object from
\code{\link[=nystrom_precond]{nystrom_precond()}}, or a function applying the inverse preconditioner
to a matrix of residuals, column by column.}

\item{tol}{Stop each system when its residual norm falls below \code{tol} times
the norm of its right-hand side.}

\item{maxit}{Maximum number of iterations.}

\item{x0}{Optional starting value, the same shape as \code{b}.}
}
\value{
An object of class \code{pcg_result} with the solution \code{x} (the same
shape as \code{b}), the \code{iterations} and whether each system \code{converged},
and the relative \code{residuals} after each iteration.
}
\description{
Solves \eqn{(A + \mu I) x = b} for a positive-semidefinite \eqn{A} using
only products with \eqn{A}. Without a preconditioner this is the ordinary
conjugate gradient method, whose iteration count grows with the condition
number; with a preconditioner from \code{\link[=nystrom_precond]{nystrom_precond()}} the count stays
small and nearly independent of the size of the problem.
}
\details{
When \code{b} is a matrix, each column is solved as a separate system, but all
of them advance together, so every step multiplies \eqn{A} by a block of
vectors. That is much faster in R than solving the columns one at a time.
}
\examples{
set.seed(1)
X <- matrix(rnorm(1000), ncol = 2)
K <- as.matrix(kernel_matrix(X))
b <- rnorm(500)
plain <- pcg(K, b, mu = 1e-3)
pre <- nystrom_precond(rpchol(K, k = 60), mu = 1e-3)
fast <- pcg(K, b, mu = 1e-3, precond = pre)
c(plain = plain$iterations, preconditioned = fast$iterations)
}
\references{
Frangella, Z., Tropp, J. A. & Udell, M. (2023) Randomized
Nystrom preconditioning. SIAM Journal on Matrix Analysis and Applications
44, 718-752. \doi{10.1137/21m1466244}
}
\seealso{
\code{\link[=nystrom_precond]{nystrom_precond()}}
}
