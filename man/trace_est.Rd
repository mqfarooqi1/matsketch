% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/trace.R
\name{trace_est}
\alias{trace_est}
\title{Stochastic trace estimation}
\usage{
trace_est(
  A,
  m,
  method = c("xtrace", "xnystrace", "hutchpp", "hutchinson"),
  n = NULL
)
}
\arguments{
\item{A}{A square matrix, a function computing \eqn{A X} for a matrix
\eqn{X}, or a lazy kernel from \code{\link[=kernel_matrix]{kernel_matrix()}}.}

\item{m}{Number of matrix-vector products to spend.}

\item{method}{Estimator; see Details.}

\item{n}{Dimension of \eqn{A}. Required only when \code{A} is a function.}
}
\value{
An object of class \code{trace_est} holding the \code{estimate}, its
\code{std_error} (not available for Hutch++), the \code{method}, and the number of
products actually used, \code{matvecs}.
}
\description{
Estimates \eqn{\mathrm{tr}(A)} for a matrix that is available only through
products \eqn{A X}, spending about \code{m} such products.
}
\details{
\itemize{
\item \code{"xtrace"} (default) combines a low-rank approximation of \eqn{A} with a
correction for what it misses, and uses every product twice through a
leave-one-out construction. It is typically far more accurate than the
older estimators at the same cost, and it reports its own standard
error. It works for any square matrix.
\item \code{"xnystrace"} is the counterpart for positive-semidefinite matrices. It
uses a Nystrom approximation, which lets it spend all \code{m} products on a
single sketch.
\item \code{"hutchpp"} is Hutch++, the estimator XTrace improves on.
\item \code{"hutchinson"} is the classical Girard-Hutchinson estimator, the average
of \eqn{\omega^\top A \omega} over random sign vectors. It is included as
the baseline the others are measured against.
}
}
\examples{
set.seed(1)
U <- qr.Q(qr(matrix(rnorm(300 * 300), 300)))
A <- U \%*\% diag((1:300)^-2) \%*\% t(U)
sum(diag(A))
trace_est(A, m = 40)
trace_est(A, m = 40, method = "hutchinson")
}
\references{
Epperly, E. N., Tropp, J. A. & Webber, R. J. (2024) XTrace: making the most
of every sample in stochastic trace estimation. SIAM Journal on Matrix
Analysis and Applications 45, 1-23. \doi{10.1137/23m1548323}

Meyer, R. A., Musco, C., Musco, C. & Woodruff, D. P. (2021) Hutch++:
optimal stochastic trace estimation. Symposium on Simplicity in Algorithms,
142-155. \doi{10.1137/1.9781611976496.16}

Hutchinson, M. F. (1989) A stochastic estimator of the trace of the
influence matrix for Laplacian smoothing splines. Communications in
Statistics - Simulation and Computation 18, 1059-1076.
\doi{10.1080/03610918908812806}
}
\seealso{
\code{\link[=diag_est]{diag_est()}}
}
