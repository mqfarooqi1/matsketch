% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/reml.R
\name{reml_exact}
\alias{reml_exact}
\title{Variance components by exact dense REML}
\usage{
reml_exact(y, G, X = NULL, start = NULL, tol = 1e-08, maxit = 100L)
}
\arguments{
\item{y}{Numeric response.}

\item{G}{A positive-semidefinite relationship matrix, or a lazy matrix from
\code{\link[=grm_matrix]{grm_matrix()}} or \code{\link[=kernel_matrix]{kernel_matrix()}}, which is formed in full.}

\item{X}{Fixed-effect design matrix. Defaults to an intercept.}

\item{start}{Starting values \code{c(genetic, residual)}. Defaults to half the
residual variance of a least-squares fit for each.}

\item{tol}{Stop when no variance component changes by more than this
fraction between iterations.}

\item{maxit}{Maximum number of REML iterations.}
}
\value{
An object of class \code{reml_sketch} whose \code{approx} is \code{"exact"}.
}
\description{
Fits the same model as \code{\link[=reml_sketch]{reml_sketch()}} with the same average-information
updates, but forms and factorizes \eqn{V} and computes every trace
exactly. It is meant for checking \code{\link[=reml_sketch]{reml_sketch()}} on problems small
enough to factorize.
}
\examples{
set.seed(1)
dat <- sim_genomic(n = 300, p = 600, h2 = 0.5)
reml_exact(dat$y, dat$G)
}
\references{
Gilmour, A. R., Thompson, R. & Cullis, B. R. (1995) Average
information REML: an efficient algorithm for variance parameter
estimation in linear mixed models. Biometrics 51, 1440-1450.
\doi{10.2307/2533274}
}
\seealso{
\code{\link[=reml_sketch]{reml_sketch()}}
}
