% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/reml.R
\name{reml_sketch}
\alias{reml_sketch}
\title{Variance components by sketched REML}
\usage{
reml_sketch(
  y,
  G,
  X = NULL,
  rank = 100L,
  m = 40L,
  approx = c("auto", "rpchol", "nystrom"),
  estimator = c("xtrace", "hutchinson"),
  start = NULL,
  tol = 1e-04,
  maxit = 50L,
  cg_tol = 1e-06
)
}
\arguments{
\item{y}{Numeric response.}

\item{G}{Relationship or kernel matrix: a positive-semidefinite matrix, a
lazy matrix from \code{\link[=grm_matrix]{grm_matrix()}} or \code{\link[=kernel_matrix]{kernel_matrix()}}, or a function
computing \eqn{G X} for a matrix \eqn{X}.}

\item{X}{Fixed-effect design matrix. Defaults to an intercept.}

\item{rank}{Rank of the approximation of \code{G} used as a preconditioner.}

\item{m}{Matrix-vector products per trace estimate. Each costs one linear
solve at every iteration.}

\item{approx}{How to approximate \code{G}. \code{"auto"} uses \code{\link[=rpchol]{rpchol()}} when entries
of \code{G} can be read and \code{\link[=nystrom]{nystrom()}} when \code{G} is a function.}

\item{estimator}{Trace estimator, \code{"xtrace"} or \code{"hutchinson"}. XTrace is
much more accurate when \eqn{PG} has a few dominant eigenvalues, as under
population structure; when the spectrum is flat the two are close.}

\item{start}{Starting values \code{c(genetic, residual)}. Defaults to half the
residual variance of a least-squares fit for each.}

\item{tol}{Stop when no variance component changes by more than this
fraction between iterations.}

\item{maxit}{Maximum number of REML iterations.}

\item{cg_tol}{Relative residual tolerance for each linear solve.}
}
\value{
An object of class \code{reml_sketch} with the variance components
\code{sigma2}, the heritability \code{h2}, their standard errors \code{se}, the
fixed effects \code{beta}, the iteration \code{history}, the number of linear
systems solved \code{solves}, and the total conjugate gradient iterations
\code{cg_iterations} they took.
}
\description{
Fits \eqn{y = X\beta + g + e}, with \eqn{g \sim N(0, \sigma^2_g G)} and
\eqn{e \sim N(0, \sigma^2_e I)}, by average-information REML, without
forming or factorizing the covariance matrix
\eqn{V = \sigma^2_g G + \sigma^2_e I}.
}
\details{
Three ideas from the package do the work.
\itemize{
\item \strong{Solves.} A system in \eqn{V} is the system
\eqn{(G + \mu I) x = b / \sigma^2_g} with \eqn{\mu = \sigma^2_e /
  \sigma^2_g}. It is solved by conjugate gradients, preconditioned with a
low-rank approximation of \eqn{G} that is built once, by \code{\link[=rpchol]{rpchol()}} or
\code{\link[=nystrom]{nystrom()}}, and reused for every value of \eqn{\mu} the fit visits. The
right-hand sides needed at each step are solved together, each starting
from its solution at the previous step.
\item \strong{One stochastic trace.} The score needs \eqn{\mathrm{tr}(PG)}, which is
estimated by XTrace or by Hutchinson's estimator. The test matrix is
drawn once and kept for the whole fit, so successive iterations see the
same sketch and converge smoothly rather than jittering.
\item \strong{One exact trace.} \eqn{PV} is idempotent with rank
\eqn{n - \mathrm{rank}(X)}, so
\eqn{\mathrm{tr}(P) = (n - \mathrm{rank}(X) - \sigma^2_g
  \mathrm{tr}(PG)) / \sigma^2_e} holds exactly, and the second trace costs
nothing.
}

Here \eqn{P = V^{-1} - V^{-1}X(X^\top V^{-1}X)^{-1}X^\top V^{-1}}. The
average-information matrix needs only quadratic forms in \eqn{P}, and these
are computed from solves rather than estimated.

Because the test matrix is fixed, the fit converges to the exact solution
of a slightly perturbed set of REML equations. The \code{trace_se} column of
\code{history} shows the size of the perturbation, which shrinks as \code{m} grows.
\code{\link[=reml_exact]{reml_exact()}} fits the same model by dense linear algebra, for checking
results on problems small enough to factorize.
}
\examples{
set.seed(1)
dat <- sim_genomic(n = 300, p = 600, h2 = 0.5)
fit <- reml_sketch(dat$y, dat$G, rank = 60, m = 30)
fit

# the relationship matrix need never be formed
reml_sketch(dat$y, grm_matrix(dat$M), rank = 60, m = 30)$h2
}
\references{
Gilmour, A. R., Thompson, R. & Cullis, B. R. (1995) Average information
REML: an efficient algorithm for variance parameter estimation in linear
mixed models. Biometrics 51, 1440-1450. \doi{10.2307/2533274}

Bermann, M., Legarra, A., Aguilar, I., Alvarez-Munera, A., Misztal, I. &
Lourenco, D. (2025) Estimation of (co)variance components for very large
datasets and complex single-step genomic models. Genetics Selection
Evolution 57. \doi{10.1186/s12711-025-01006-9}
}
\seealso{
\code{\link[=reml_exact]{reml_exact()}}, \code{\link[=grm_matrix]{grm_matrix()}}, \code{\link[=sim_genomic]{sim_genomic()}}
}
