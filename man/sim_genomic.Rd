% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/simulate.R
\name{sim_genomic}
\alias{sim_genomic}
\title{Simulate a genomic data set}
\usage{
sim_genomic(
  n = 1000L,
  p = 2000L,
  h2 = 0.5,
  pops = 1L,
  fst = 0.05,
  form_G = TRUE
)
}
\arguments{
\item{n}{Number of individuals.}

\item{p}{Number of markers.}

\item{h2}{Heritability, between 0 and 1.}

\item{pops}{Number of subpopulations.}

\item{fst}{Fixation index between subpopulations, used when \code{pops > 1}.}

\item{form_G}{Return the relationship matrix itself. Set to \code{FALSE} for
large \code{n} and pass \code{grm_matrix(M)} to the fitting functions instead.}
}
\value{
A list with the phenotype \code{y}, an intercept design matrix \code{X}, the
genotype matrix \code{M}, the relationship matrix \code{G} (when \code{form_G = TRUE}),
the subpopulation of each individual \code{pop}, and the true \code{h2}.
}
\description{
Draws biallelic marker genotypes, forms the genomic relationship matrix of
VanRaden (2008), and simulates a trait with the requested heritability, so
that \code{\link[=reml_sketch]{reml_sketch()}} can be checked against known variance components.
}
\details{
With \code{pops > 1} the individuals come from that many subpopulations whose
allele frequencies have drifted apart under the Balding-Nichols model with
fixation index \code{fst}. Population structure gives the relationship matrix a
few large eigenvalues, as real breeding and human cohorts do, and those are
exactly what a low-rank preconditioner captures.

Genetic values are sums of marker effects on the centred genotypes, with
variance chosen so that the genetic and residual variance components on the
scale of the relationship matrix are \code{h2} and \code{1 - h2}.

The function draws random numbers but does not set the seed; call
\code{\link[base:set.seed]{base::set.seed()}} first for a reproducible data set.
}
\examples{
set.seed(1)
dat <- sim_genomic(n = 200, p = 500, h2 = 0.4, pops = 3)
dim(dat$G)
table(dat$pop)
}
\references{
VanRaden, P. M. (2008) Efficient methods to compute genomic predictions.
Journal of Dairy Science 91, 4414-4423. \doi{10.3168/jds.2007-0980}

Balding, D. J. & Nichols, R. A. (1995) A method for quantifying
differentiation between populations at multi-allelic loci and its
implications for investigating identity and paternity. Genetica 96, 3-12.
\doi{10.1007/bf01441146}
}
\seealso{
\code{\link[=grm_matrix]{grm_matrix()}}, \code{\link[=reml_sketch]{reml_sketch()}}
}
